{
  "name": "flour-grain-inventory-sync",
  "version": "1.0.0",
  "description": "Unified grain-flour inventory sync: Square (truth) ↔ BigCommerce (calculated)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "register-webhook": "node register-webhook.js",
    "test": "node test-local.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "node-fetch": "^2.7.0",
        "nodemailer": "^6.9.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
